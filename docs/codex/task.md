# Current Task SSOT

## Objective

Continue from the A-005 runtime backlog and implement the minimal real spawn boundary for T-007.5 while preserving the default skeleton-only `run` / `orchestrate` behavior.

## Scope

- Edit `/Users/suzukimakoto/plugins/coding-agents/bin/coding-agents.mjs` using Node.js standard libraries only.
- Update `docs/codex/*.md` records needed for implementation and audit.
- Keep `skills/coding-agents/SKILL.md` unchanged unless the source CLI contract cannot remain honest without it.
- Preserve existing uncommitted docs/codex intake changes.
- Do not commit.

## Non-Goals

- Do not edit `~/.codex/plugins/cache/`.
- Do not edit `~/.agents/plugins/marketplace.json`.
- Do not add dependencies.
- Do not run `npm install`, `pip`, `python`, or `python3`.
- Do not implement MCP servers, hooks, app integration, or a broad runner framework.
- Do not touch files outside `/Users/suzukimakoto/plugins/coding-agents`.
- Do not edit `skills/coding-agents/SKILL.md` unless the CLI surface contract truly requires it.

## Completion Conditions

- `verify-assignments` and `doctor` reject missing or empty assignment isolation keys.
- `assign` records scoped assignments with `task_id`, `epoch`, and `scope`.
- `collect` records concise parent-integration packets.
- `run` / `orchestrate` keeps default `spawned: false` skeleton behavior.
- `run --runner codex-cli` spawns non-interactive local `codex exec` with cwd from `--cwd`, no interactive approval, conservative sandbox settings, strict scoped child prompt, timeout handling, and temporary last-message capture.
- `docs/codex/runner.md` records normalized runner results with `spawned`, runner name, exit code, concise summary, and failure details.
- Syntax check, assignment verification, doctor, and diff checks are run before handoff.
- Marketplace/cache activation remains deferred.

<!-- coding-agents-mvp:start -->
# Generated by coding-agents MVP

# Active Task

- task_id: CA-RUNNER-SPAWN-001
- epoch: 2026-05-11T01
- scope: /Users/suzukimakoto/plugins/coding-agents
- task: Select and implement a callable runner boundary for real subagent process spawning after the CLI skeleton

## Completion Conditions

- `docs/codex` files exist.
- 14 role assignments include `role`, `status`, `task_id`, `epoch`, `scope`, `assignment`, and `expected_output`.
- `run` / `orchestrate` defaults to `spawned: false`.
- `run --runner codex-cli` records a normalized process runner result without dumping long logs.
- Handoff prompt is available for the next worker.
<!-- coding-agents-mvp:end -->

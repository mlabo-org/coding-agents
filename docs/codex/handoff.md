# MVP Handoff Contract

The runnable MVP must generate `docs/codex/handoff.md` in the target project.

The handoff must tell the next worker:

- the active `task_id`
- the target `cwd`
- the task text
- the active `epoch`
- the allowed `scope`
- the required `docs/codex` read order
- the requirement to preserve unrelated edits and update audit notes

## Current Verification Handoff

- task_id: `CA-RUNNER-DOCS-FINALIZE-001`
- epoch: `2026-05-11T06`
- scope: `docs/codex/*.md` inspection; minimal edits only to `audit.md`, `todo.md`, and `handoff.md`
- current docs state:
  - Runner smoke verification is already recorded in A-009 and `docs/codex/runner.md`.
  - Refresh succeeded by script using bundled Python: source `/Users/suzukimakoto/plugins/coding-agents` version `0.1.0` refreshed to cache `/Users/suzukimakoto/.codex/plugins/cache/local-plugins/coding-agents/0.1.0`, with old cache backup `/Users/suzukimakoto/.codex/plugins/cache/local-plugins/coding-agents.refresh-backup-20260511-052853`.
  - Source/cache `plugin.json`, `bin/coding-agents.mjs`, and `docs/codex/runner.md` match after refresh.
  - New-session approximation used `fork_context=false` `@coding-agents` plus cache artifact verification; cache CLI help lists `intake`, `assign`, `collect`, `run`, `verify-assignments`, `handoff`, and `doctor`; cache `node --check`, `verify-assignments`, and `doctor` passed.
  - Smoke temp cwd `/tmp/coding-agents-smoke.DSOu6j`: default `run` passed with `spawned: false`; after `git init`, `run --runner codex-cli` passed with `spawned: true`, `exit_code: 0`, `status: completed`, and child summary `codex-cli-smoke-ok`; final `verify-assignments` passed.
- remaining limit: this is not a true fresh Codex app restart/thread proof.
- boundaries honored: did not edit `bin/coding-agents.mjs`, `skills/coding-agents/SKILL.md`, or cache.

<!-- coding-agents-mvp:start -->
# Generated by coding-agents MVP

# Handoff Prompt

You are a coding-agents worker for task `CA-RUNNER-SPAWN-001`.

- cwd: /Users/suzukimakoto/plugins/coding-agents
- task: Select and implement a callable runner boundary for real subagent process spawning after the CLI skeleton
- epoch: 2026-05-11T01
- scope: /Users/suzukimakoto/plugins/coding-agents

Read `docs/codex/README.md`, then `project.md`, `task.md`, `todo.md`, `decisions.md`, `assignments.md`, `runner.md`, and `audit.md`.
Preserve unrelated edits. Work only inside scope. Update `docs/codex/audit.md` with verification results before handoff.
<!-- coding-agents-mvp:end -->
